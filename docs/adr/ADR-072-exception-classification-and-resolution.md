# ADR-072: Exception classification and resolution

Accepted. Structured code and severity drive operations. Each active mismatch is a single investigation with append-only audit. Assignment, ignore and resolution require actor, reason code and comment. High-risk financial actions need future maker-checker approval through the owning service; resolving the ticket does not mutate financial records.
